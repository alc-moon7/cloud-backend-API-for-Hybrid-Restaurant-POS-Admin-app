NODE_ENV=development
PORT=4000
DATABASE_URL=postgres://hybrid_pos:hybrid_pos_password@localhost:5432/hybrid_pos

# Optional. If set, all non-health API routes require:
# Authorization: Bearer <DEVICE_API_TOKEN>
DEVICE_API_TOKEN=

# Browser/app origins. Use * for MVP.
CORS_ORIGIN=*
